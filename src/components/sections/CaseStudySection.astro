<section
  id="central-hub-case-study"
  class="case-study-preview"
>
  <div class="container case-study-grid">
    <div>
      <p class="eyebrow">
        Featured Case Study
      </p>

      <h2>
        Central Hub, from idea to production.
      </h2>
    </div>

    <div>
      <p>
        The full case study covers the problem, architecture,
        PostgreSQL data layer, Raspberry Pi deployment,
        monitoring, access controls, engineering decisions,
        challenges, and future improvements.
      </p>

      <a class="case-study-link" href="/projects/central-hub">
        Explore the full case study
        <span>→</span>
      </a>
    </div>
  </div>
</section>

<style>
  .case-study-preview {
    padding: 8rem 0;
    border-top: 1px solid var(--border);
  }

  .case-study-grid {
    display: grid;
    grid-template-columns: 1fr 0.7fr;
    gap: 6rem;
  }

  .case-study-grid h2 {
    margin: 1rem 0 0;
    font-size: clamp(2.8rem, 5vw, 5rem);
    line-height: 0.98;
    letter-spacing: -0.055em;
  }

  .case-study-grid p {
    color: var(--text-muted);
    line-height: 1.85;
  }

  .case-study-link {
    display: inline-flex;
    gap: 0.7rem;
    margin-top: 1rem;
    color: #9989ff;
  }

  @media (max-width: 1050px) {
    .case-study-grid {
      grid-template-columns: 1fr;
    }
  }
</style>