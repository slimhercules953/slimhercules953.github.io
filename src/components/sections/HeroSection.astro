<section class="hero-section">
  <div class="hero-orb hero-orb-one"></div>
  <div class="hero-orb hero-orb-two"></div>

  <div class="container hero-grid">
    <div class="hero-copy">
      <p class="eyebrow hero-eyebrow">
        <span class="eyebrow-line"></span>
        Etter Digital / Infrastructure & Software
      </p>

      <h1>
        Build.
        <br />
        Deploy.
        <br />
        <span>Operate.</span>
      </h1>

      <p class="hero-description">
        I build and operate systems spanning infrastructure,
        networking, automation, self-hosting, and software.
      </p>

      <div class="hero-actions">
        <a class="primary-action" href="#projects">
          Explore Projects
          <span>↓</span>
        </a>

        <a
          class="secondary-action"
          href="https://github.com/slimhercules953"
          target="_blank"
          rel="noopener noreferrer"
        >
          View GitHub
          <span>↗</span>
        </a>
      </div>

      <div class="hero-tech">
        <span>Linux</span>
        <span>Networking</span>
        <span>Next.js</span>
        <span>PostgreSQL</span>
        <span>Raspberry Pi</span>
      </div>
    </div>

    <div class="system-showcase">
      <div class="showcase-glow"></div>

      <div class="showcase-frame">
        <div class="window-bar">
          <div class="window-controls" aria-hidden="true">
            <span></span>
            <span></span>
            <span></span>
          </div>

          <div class="window-address mono">
            dashboard.etterdigital.dev
          </div>

          <div class="window-secure mono">
            <span class="secure-dot"></span>
            PRIVATE
          </div>
        </div>

        <div class="dashboard-shell">
          <aside class="dashboard-sidebar">
            <div class="mini-brand">
              <span>ED</span>
            </div>

            <div class="sidebar-items">
              <span class="sidebar-item active"></span>
              <span class="sidebar-item"></span>
              <span class="sidebar-item"></span>
              <span class="sidebar-item"></span>
            </div>

            <span class="sidebar-item sidebar-bottom"></span>
          </aside>

          <div class="dashboard-content">
            <div class="dashboard-heading">
              <div>
                <p class="dashboard-label mono">
                  PERSONAL OPERATIONS PLATFORM
                </p>

                <h2>Central Hub</h2>
              </div>

              <div class="operational-pill">
                <span></span>
                Operational
              </div>
            </div>

            <div class="metrics-grid">
              <article class="metric-card">
                <div class="metric-icon purple">P</div>

                <div>
                  <span class="metric-value">Projects</span>
                  <span class="metric-label">Management</span>
                </div>
              </article>

              <article class="metric-card">
                <div class="metric-icon blue">S</div>

                <div>
                  <span class="metric-value">Systems</span>
                  <span class="metric-label">Monitoring</span>
                </div>
              </article>

              <article class="metric-card">
                <div class="metric-icon green">✓</div>

                <div>
                  <span class="metric-value">Tasks</span>
                  <span class="metric-label">Workflow</span>
                </div>
              </article>
            </div>

            <div class="dashboard-lower">
              <div class="activity-panel">
                <div class="panel-heading">
                  <span>Platform Components</span>
                  <span class="mono">ACTIVE</span>
                </div>

                <div class="activity-row">
                  <span class="activity-dot purple"></span>

                  <div>
                    <strong>Central Hub</strong>
                    <small>Private operations dashboard</small>
                  </div>

                  <span class="activity-status">Online</span>
                </div>

                <div class="activity-row">
                  <span class="activity-dot blue"></span>

                  <div>
                    <strong>Pi Monitor</strong>
                    <small>Infrastructure metrics integration</small>
                  </div>

                  <span class="activity-status">Connected</span>
                </div>

                <div class="activity-row">
                  <span class="activity-dot green"></span>

                  <div>
                    <strong>PostgreSQL</strong>
                    <small>Application data persistence</small>
                  </div>

                  <span class="activity-status">Active</span>
                </div>
              </div>

              <div class="stack-panel">
                <span class="stack-label mono">CORE STACK</span>

                <div class="stack-list">
                  <span>Next.js</span>
                  <span>Prisma</span>
                  <span>PostgreSQL</span>
                  <span>Linux</span>
                  <span>PM2</span>
                </div>

                <small>Self-hosted on Raspberry Pi</small>
              </div>
            </div>
          </div>
        </div>

        <div class="showcase-footer mono">
          <span>
            <span class="status-dot"></span>
            RASPBERRY PI / ONLINE
          </span>

          <span>PRIVATE OPERATIONS ENVIRONMENT</span>
        </div>
      </div>
    </div>
  </div>

  <div class="container hero-bottom">
    <div class="hero-bottom-line"></div>

    <div class="hero-status">
      <span>
        <span class="status-dot"></span>
        ACTIVE INFRASTRUCTURE
      </span>

      <span>VIRGINIA / EST</span>
      <span>ETTER DIGITAL / 2026</span>
    </div>
  </div>
</section>

<style>
  .hero-section {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    padding: 6.5rem 0 2.25rem;
  }

  .hero-orb {
    position: absolute;
    z-index: -1;
    border-radius: 999px;
    filter: blur(80px);
    pointer-events: none;
  }

  .hero-orb-one {
    top: 2rem;
    left: 33%;
    width: 28rem;
    height: 28rem;
    background: rgba(103, 71, 255, 0.11);
  }

  .hero-orb-two {
    right: -10rem;
    top: 13rem;
    width: 30rem;
    height: 30rem;
    background: rgba(52, 96, 255, 0.08);
  }

  .hero-grid {
    display: grid;
    grid-template-columns:
      minmax(0, 0.82fr)
      minmax(520px, 1.18fr);
    gap: clamp(3rem, 6vw, 7rem);
    align-items: center;
  }

  .hero-copy {
    position: relative;
    z-index: 2;
  }

  .hero-eyebrow {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    margin: 0 0 1.5rem;
  }

  .eyebrow-line {
    width: 2.5rem;
    height: 1px;
    background: var(--accent);
    box-shadow: 0 0 12px rgba(124, 92, 255, 0.7);
  }

  h1 {
    margin: 0;
    font-size: clamp(4.8rem, 8vw, 8.7rem);
    line-height: 0.83;
    letter-spacing: -0.078em;
    text-transform: uppercase;
    font-weight: 900;
  }

  h1 span {
    display: inline-block;
    background: linear-gradient(
      110deg,
      #ffffff 0%,
      #a99cff 25%,
      #7757ff 60%,
      #4f7cff 100%
    );
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .hero-description {
    max-width: 610px;
    margin: 2rem 0 0;
    color: var(--text-muted);
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    line-height: 1.8;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }

  .primary-action,
  .secondary-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    padding: 0.85rem 1.15rem;
    border-radius: 9px;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.055em;
    text-transform: uppercase;
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      background 180ms ease;
  }

  .primary-action {
    border: 1px solid rgba(124, 92, 255, 0.72);
    background: linear-gradient(
      135deg,
      rgba(124, 92, 255, 0.22),
      rgba(80, 66, 180, 0.1)
    );
  }

  .secondary-action {
    border: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.018);
    color: var(--text-muted);
  }

  .primary-action:hover,
  .secondary-action:hover {
    transform: translateY(-2px);
    color: white;
    border-color: rgba(124, 92, 255, 0.7);
  }

  .hero-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    margin-top: 2.25rem;
  }

  .hero-tech span {
    padding: 0.4rem 0.58rem;
    border: 1px solid rgba(124, 92, 255, 0.2);
    border-radius: 5px;
    background: rgba(124, 92, 255, 0.025);
    color: #978da9;
    font-family: var(--font-mono);
    font-size: 0.6rem;
    letter-spacing: 0.055em;
    text-transform: uppercase;
  }

  .system-showcase {
    position: relative;
    min-width: 0;
    perspective: 1400px;
  }

  .showcase-glow {
    position: absolute;
    inset: 15% 10%;
    z-index: -1;
    border-radius: 50%;
    background: rgba(105, 72, 255, 0.19);
    filter: blur(70px);
  }

  .showcase-frame {
    overflow: hidden;
    border: 1px solid rgba(139, 112, 255, 0.3);
    border-radius: 14px;
    background: rgba(7, 9, 15, 0.94);
    box-shadow:
      0 40px 100px rgba(0, 0, 0, 0.5),
      0 0 80px rgba(79, 58, 180, 0.09);
    transform: rotateY(-2deg) rotateX(1deg);
    transition:
      transform 400ms ease,
      border-color 400ms ease;
  }

  .system-showcase:hover .showcase-frame {
    transform: rotateY(0deg) rotateX(0deg) translateY(-5px);
    border-color: rgba(139, 112, 255, 0.5);
  }

  .window-bar {
    min-height: 42px;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    padding: 0 0.9rem;
    border-bottom: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.018);
  }

  .window-controls {
    display: flex;
    gap: 0.4rem;
  }

  .window-controls span {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #3a3f4b;
  }

  .window-controls span:first-child {
    background: #7958ff;
  }

  .window-address {
    color: #737c8d;
    font-size: 0.56rem;
  }

  .window-secure {
    justify-self: end;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    color: #687181;
    font-size: 0.5rem;
    letter-spacing: 0.08em;
  }

  .secure-dot,
  .status-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--status-online);
    box-shadow: 0 0 8px rgba(62, 240, 143, 0.5);
  }

  .status-dot {
    display: inline-block;
  }

  .dashboard-shell {
    min-height: 390px;
    display: grid;
    grid-template-columns: 55px minmax(0, 1fr);
  }

  .dashboard-sidebar {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1rem 0;
    border-right: 1px solid var(--border);
  }

  .mini-brand {
    display: grid;
    place-items: center;
    width: 27px;
    height: 27px;
    border: 1px solid rgba(124, 92, 255, 0.5);
    border-radius: 6px;
    color: #a99cff;
    font-size: 0.55rem;
    font-weight: 800;
  }

  .sidebar-items {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
    margin-top: 2rem;
  }

  .sidebar-item {
    display: block;
    width: 15px;
    height: 15px;
    border: 1px solid #343a46;
    border-radius: 4px;
  }

  .sidebar-item.active {
    border-color: #8064ff;
    background: rgba(124, 92, 255, 0.18);
  }

  .sidebar-bottom {
    margin-top: auto;
  }

  .dashboard-content {
    padding: 1.4rem;
  }

  .dashboard-heading {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
  }

  .dashboard-label {
    margin: 0;
    color: #706a85;
    font-size: 0.48rem;
  }

  .dashboard-heading h2 {
    margin: 0.25rem 0 0;
  }

  .operational-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.36rem 0.55rem;
    border: 1px solid rgba(62, 240, 143, 0.18);
    border-radius: 999px;
    color: #8ec9a6;
    font-family: var(--font-mono);
    font-size: 0.48rem;
    text-transform: uppercase;
  }

  .operational-pill span {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--status-online);
  }

  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.65rem;
    margin-top: 1.4rem;
  }

  .metric-card {
    display: flex;
    gap: 0.65rem;
    align-items: center;
    padding: 0.85rem;
    border: 1px solid var(--border);
    border-radius: 8px;
  }

  .metric-icon {
    display: grid;
    place-items: center;
    width: 27px;
    height: 27px;
    border-radius: 6px;
  }

  .metric-icon.purple {
    background: rgba(124, 92, 255, 0.12);
  }

  .metric-icon.blue {
    background: rgba(79, 124, 255, 0.12);
  }

  .metric-icon.green {
    background: rgba(62, 240, 143, 0.09);
  }

  .metric-value,
  .metric-label {
    display: block;
  }

  .metric-value {
    font-size: 0.7rem;
  }

  .metric-label {
    color: #6e7686;
    font-size: 0.46rem;
  }

  .dashboard-lower {
    display: grid;
    grid-template-columns: 1.55fr 0.7fr;
    gap: 0.65rem;
    margin-top: 0.65rem;
  }

  .activity-panel,
  .stack-panel {
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.85rem;
  }

  .panel-heading {
    display: flex;
    justify-content: space-between;
    padding-bottom: 0.7rem;
    border-bottom: 1px solid var(--border);
    font-size: 0.58rem;
  }

  .activity-row {
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.55rem;
    align-items: center;
    padding: 0.65rem 0;
  }

  .activity-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
  }

  .activity-dot.purple {
    background: #8b70ff;
  }

  .activity-dot.blue {
    background: #5f8cff;
  }

  .activity-dot.green {
    background: #49dd8c;
  }

  .activity-row strong,
  .activity-row small {
    display: block;
  }

  .activity-row strong {
    font-size: 0.55rem;
  }

  .activity-row small {
    color: #5f6673;
    font-size: 0.42rem;
  }

  .activity-status {
    color: #66c78f;
    font-size: 0.42rem;
  }

  .stack-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
    margin: 0.8rem 0;
  }

  .stack-list span {
    color: #877da1;
    font-size: 0.42rem;
  }

  .stack-panel small {
    color: #626a78;
    font-size: 0.45rem;
  }

  .showcase-footer {
    display: flex;
    justify-content: space-between;
    padding: 0.65rem 0.9rem;
    border-top: 1px solid var(--border);
    color: #656d7b;
    font-size: 0.46rem;
  }

  .showcase-footer span {
    display: flex;
    gap: 0.45rem;
    align-items: center;
  }

  .hero-bottom {
    margin-top: 5rem;
  }

  .hero-bottom-line {
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(124, 92, 255, 0.35),
      transparent
    );
  }

  .hero-status {
    display: flex;
    justify-content: space-between;
    padding-top: 1rem;
    color: #565e6d;
    font-family: var(--font-mono);
    font-size: 0.53rem;
  }

  .hero-status span {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  @media (max-width: 1180px) {
    .hero-grid {
      grid-template-columns: 0.9fr 1.1fr;
      gap: 3rem;
    }
  }

  @media (max-width: 1050px) {
    .hero-grid {
      grid-template-columns: 1fr;
    }

    .system-showcase {
      width: min(100%, 820px);
    }
  }

  @media (max-width: 680px) {
    .hero-section {
      padding-top: 3.5rem;
    }

    h1 {
      font-size: clamp(4rem, 19vw, 6.3rem);
    }

    .hero-actions {
      flex-direction: column;
    }

    .primary-action,
    .secondary-action {
      width: 100%;
    }

    .dashboard-shell {
      grid-template-columns: 40px 1fr;
    }

    .metrics-grid,
    .dashboard-lower {
      grid-template-columns: 1fr;
    }

    .stack-panel {
      display: none;
    }

    .window-address {
      display: none;
    }
  }
</style>